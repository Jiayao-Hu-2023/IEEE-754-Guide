# 理解 IEEE 754 浮点数转换
## 🧮 分步指南与示例（单精度和双精度）

> 📘 **目标读者：** 计算机科学专业学生、开发人员，以及所有对计算机如何表示实数感到好奇的人。
>
> 👨‍🎓 **作者:** Jiayao Hu


[English Version](<IEEE 754 Conversion Examples.md>) | [中文版本](<IEEE 754 Conversion Examples zh.md>)

---

## 1. 🎯 什么是 IEEE 754？

**IEEE 754 标准**定义了浮点数在计算机中的存储方式。它规定了两种常见格式：

| 格式 | 总位数 | 符号位 | 指数位 | 尾数（小数部分） | 偏置值 |
|------|--------|--------|--------|------------------|--------|
| **单精度 (SP)** | 32 | 1 位 | 8 位 | 23 位 | 127 |
| **双精度 (DP)** | 64 | 1 位 | 11 位 | 52 位 | 1023 |

所表示数值的通用公式为：

$$
\text{值} = (-1)^{\text{符号}} \times 1.\text{尾数} \times 2^{\text{指数} - \text{偏置值}}
$$

> 💡 **关键点：** 对于规格化数，尾数中的前导 `1.` 是*隐含*的（隐藏位）。我们只存储二进制小数点后的小数部分。

---

## 2. 🔄 转换算法：十进制 → IEEE 754

要将十进制数转换为 IEEE 754 格式，请按以下步骤操作：

### 分步过程

1. **🔢 将绝对值转换为二进制**
   - 用除 2 取余法转换整数部分。
   - 用乘 2 取整法转换小数部分。

2. **📐 规范化二进制数**
   - 移动二进制小数点，使其左边恰好只有一个 `1`。
   - 表示为： $1.\text{小数部分} \times 2^{\text{指数}}$

3. **➕ 确定各字段**
   - **符号位：** 正数为 `0`，负数为 `1`。
   - **指数：** 将真实指数加上偏置值（单精度 127，双精度 1023），然后转换为二进制。
   - **尾数：** 取前导 `1.` 之后的小数部分，并在右侧补零，填满 23 位（单精度）或 52 位（双精度）。

4. **🧩 拼接二进制串**
   - 按顺序连接：`[符号][指数][尾数]`

---

## 3. 📚 示例 1：转换 `9.75`

将十进制数 $9.75$ 转换为单精度和双精度。

### 步骤 1：转换为二进制

- 整数部分： $9_{10} = 1001_2$
- 小数部分： $0.75 \times 2 = 1.5$ → `1`
  - $0.5 \times 2 = 1.0$ → `1`
  - $0.0$ → 停止

所以：

$$
9.75_{10} = 1001.11_2
$$

### 步骤 2：规范化

$$
1001.11_2 = 1.00111_2 \times 2^3
$$

此处**真实指数**为 $E = 3$。

### 步骤 3：确定各字段

| 字段 | 计算 | 值 |
|------|------|-----|
| **符号** | 正数 | `0` |
| **单精度指数** | $3 + 127 = 130$ | `10000010` |
| **双精度指数** | $3 + 1023 = 1026$ | `10000000010` |
| **尾数** | `00111` + 补零 | `00111000000000000000000`（单精度）/ `0011100...0`（双精度，52 位） |

### 步骤 4：拼接

**单精度（32 位）：**

$$
\underbrace{0}_{\text{符号}}\ \underbrace{10000010}_{\text{指数}}\ \underbrace{00111000000000000000000}_{\text{尾数}}
$$

**十六进制：** `0x41180000`

**双精度（64 位）：**

$$
\underbrace{0}_{\text{符号}}\ \underbrace{10000000010}_{\text{指数}}\ \underbrace{0011100000000000000000000000000000000000000000000000}_{\text{尾数}}
$$

**十六进制：** `0x4023800000000000`

> ✅ **验证：** $(-1)^0 \times 1.00111_2 \times 2^3 = 1001.11_2 = 9.75_{10}$ ✓

---

## 4. 📚 示例 2：转换 `-42.625`

现在处理一个**负数**，同时包含整数和小数部分。

### 步骤 1：转换为二进制

- 整数部分： $42_{10} = 101010_2$
- 小数部分： $0.625 \times 2 = 1.25$ → `1`
  - $0.25 \times 2 = 0.5$ → `0`
  - $0.5 \times 2 = 1.0$ → `1`

所以：

$$
42.625_{10} = 101010.101_2
$$

### 步骤 2：规范化

$$
101010.101_2 = 1.01010101_2 \times 2^5
$$

**真实指数**为 $E = 5$。

### 步骤 3：确定各字段

| 字段 | 计算 | 值 |
|------|------|-----|
| **符号** | 负数 | `1` |
| **单精度指数** | $5 + 127 = 132$ | `10000100` |
| **双精度指数** | $5 + 1023 = 1028$ | `10000000100` |
| **尾数** | `01010101` + 补零 | `01010101000000000000000`（单精度）/ `0101010100...0`（双精度，52 位） |

### 步骤 4：拼接

**单精度（32 位）：**

$$
\underbrace{1}_{\text{符号}}\ \underbrace{10000100}_{\text{指数}}\ \underbrace{01010101000000000000000}_{\text{尾数}}
$$

**十六进制：** `0xC22A8000`

**双精度（64 位）：**

$$
\underbrace{1}_{\text{符号}}\ \underbrace{10000000100}_{\text{指数}}\ \underbrace{0101010100000000000000000000000000000000000000000000}_{\text{尾数}}
$$

**十六进制：** `0xC045500000000000`

> ✅ **验证：** $(-1)^1 \times 1.01010101_2 \times 2^5 = -101010.101_2 = -42.625_{10}$ ✓

---

## 5. 📚 示例 3：转换 `0.15625`

此示例演示如何处理**纯小数**（值 < 1），其指数为**负数**。

### 步骤 1：转换为二进制

- 整数部分： $0_{10} = 0_2$
- 小数部分：
  - $0.15625 \times 2 = 0.3125$ → `0`
  - $0.3125 \times 2 = 0.625$ → `0`
  - $0.625 \times 2 = 1.25$ → `1`
  - $0.25 \times 2 = 0.5$ → `0`
  - $0.5 \times 2 = 1.0$ → `1`

所以：

$$
0.15625_{10} = 0.00101_2
$$

### 步骤 2：规范化

$$
0.00101_2 = 1.01_2 \times 2^{-3}
$$

**真实指数**为 $E = -3$。

### 步骤 3：确定各字段

| 字段 | 计算 | 值 |
|------|------|-----|
| **符号** | 正数 | `0` |
| **单精度指数** | $-3 + 127 = 124$ | `01111100` |
| **双精度指数** | $-3 + 1023 = 1020$ | `01111111100` |
| **尾数** | `01` + 补零 | `01000000000000000000000`（单精度）/ `010000...0`（双精度，52 位） |

### 步骤 4：拼接

**单精度（32 位）：**

$$
\underbrace{0}_{\text{符号}}\ \underbrace{01111100}_{\text{指数}}\ \underbrace{01000000000000000000000}_{\text{尾数}}
$$

**十六进制：** `0x3E200000`

**双精度（64 位）：**

$$
\underbrace{0}_{\text{符号}}\ \underbrace{01111111100}_{\text{指数}}\ \underbrace{0100000000000000000000000000000000000000000000000000}_{\text{尾数}}
$$

**十六进制：** `0x3FC4000000000000`

> ✅ **验证：** $1.01_2 \times 2^{-3} = 0.00101_2 = 0.15625_{10}$ ✓

---

## 6. 🔄 将 IEEE 754 转换回十进制

现在反过来！给定一个 IEEE 754 数的十六进制表示，如何恢复其十进制值？

### 算法

1. **🔍 将十六进制转为二进制**，并拆分为 [符号 | 指数 | 尾数]。
2. **📊 计算真实指数：** $\text{真实指数} = \text{二进制指数} - \text{偏置值}$
3. **🔢 重构有效数：** $1.\text{尾数}_2$（插入隐藏位）。
4. **🧮 应用公式：**

$$
\text{值} = (-1)^{\text{符号}} \times (1 + \sum_{i=1}^{n} m_i \cdot 2^{-i}) \times 2^{\text{真实指数}}
$$

---

## 7. 📚 示例 4：单精度 → 十进制（`0xC1480000`）

### 步骤 1：转换为二进制

$$
\text{0xC1480000} = 1100\ 0001\ 0100\ 1000\ 0000\ 0000\ 0000\ 0000_2
$$

拆分为各字段：
- **符号：** `1`（第 31 位）
- **指数：** `10000010`（第 30–23 位）
- **尾数：** `10010000000000000000000`（第 22–0 位）

### 步骤 2：计算真实指数

$$
\text{二进制指数} = 10000010_2 = 130_{10}
$$

$$
\text{真实指数} = 130 - 127 = 3
$$

### 步骤 3：重构有效数

含隐藏位：

$$
1.10010000000000000000000_2
$$

### 步骤 4：计算数值

$$
\text{值} = (-1)^1 \times 1.1001_2 \times 2^3 = -1100.1_2
$$

转换为十进制：

$$
-1100.1_2 = -(8 + 4 + 0.5) = \boxed{-12.5_{10}}
$$

> 🎉 结果：`0xC1480000`（单精度）表示 **-12.5**

---

## 8. 📚 示例 5：双精度 → 十进制（`0x4024000000000000`）

### 步骤 1：转换为二进制

$$
\text{0x4024000000000000} = 0100\ 0000\ 0010\ 0100\ 0000\ ...\ 0000_2
$$

拆分为各字段：
- **符号：** `0`（第 63 位）
- **指数：** `10000000010`（第 62–52 位）
- **尾数：** `0100000000000000000000000000000000000000000000000000`（第 51–0 位）

### 步骤 2：计算真实指数

$$
\text{二进制指数} = 10000000010_2 = 1026_{10}
$$

$$
\text{真实指数} = 1026 - 1023 = 3
$$

### 步骤 3：重构有效数

含隐藏位：

$$
1.0100000000000000000000000000000000000000000000000000_2
$$

### 步骤 4：计算数值

$$
\text{值} = (-1)^0 \times 1.01_2 \times 2^3 = 1010_2
$$

等等——更精确地说，尾数是 `01`，所以：

$$
1.01_2 = 1 + 0 \times 2^{-1} + 1 \times 2^{-2} = 1 + 0.25 = 1.25_{10}
$$

然后：

$$
1.25 \times 2^3 = 1.25 \times 8 = \boxed{10.0_{10}}
$$

实际上再核对一下： $1.01_2 \times 2^3 = 1010_2 = 8 + 2 = 10_{10}$。✓

> 🎉 结果：`0x4024000000000000`（双精度）表示 **10.0**

---

## 9. 📝 快速参考备忘单

| 任务 | 公式 / 规则 |
|------|-------------|
| **单精度偏置值** | $127$ |
| **双精度偏置值** | $1023$ |
| **单精度指数位数** | $8$（范围： $-126$ 到 $+127$） |
| **双精度指数位数** | $11$（范围： $-1022$ 到 $+1023$） |
| **规格化形式** | $1.\text{ffff} \times 2^{E}$ |
| **十进制值** | $(-1)^{S} \times (1 + M) \times 2^{E - \text{偏置值}}$ |

### 需要记住的特殊值 🚨

| 模式 | 含义 |
|------|------|
| 指数全为 0，尾数为 0 | $\pm 0$ |
| 指数全为 0，尾数 $\neq$ 0 | 非规格化数 |
| 指数全为 1，尾数为 0 | $\pm \infty$ |
| 指数全为 1，尾数 $\neq$ 0 | NaN（非数值） |

---

## 10. 🎯 总结

| 示例 | 十进制 | 单精度十六进制 | 双精度十六进制 |
|------|--------|----------------|----------------|
| 1 | $9.75$ | `0x41180000` | `0x4023800000000000` |
| 2 | $-42.625$ | `0xC22A8000` | `0xC045500000000000` |
| 3 | $0.15625$ | `0x3E200000` | `0x3FC4000000000000` |
| 4 | $-12.5$ | `0xC1480000` | — |
| 5 | $10.0$ | — | `0x4024000000000000` |

> 💻 **专业提示：** 使用 Python 的 `struct.pack('!f', value)` 或在线 IEEE 754 转换器来验证您的手算结果！

---

*🚀 如果您觉得本指南有帮助，请在 GitHub 上给它一个 ⭐！*
